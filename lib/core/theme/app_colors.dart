import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // --- BRAND COLORS ---
  static const Color navy = Color(0xFF262F59);
  static const Color navyDark = Color(0xFF1A1F3A);
  static const Color cyan = Color(0xFF12A7CD);
  static const Color orange = Color(0xFFEF7F1A);

  // --- BACKGROUNDS ---
  // Light
  static const Color bgLight = Color(0xFFF0F2F5);
  static const Color surfaceLight = Color(0xFFFFFFFF);
  static const Color inputLight = Color(0xFFFAFAFA);

  // Dark
  static const Color bgDark = Color(0xFF0D1117);
  static const Color surfaceDark = Color(0xFF161B22);
  static const Color inputDark = Color(0xFF21262D);

  // --- TEXT ---
  static const Color textPrimary = Color(0xFF0D1B3E);
  static const Color textSecondary = Color(0xFF262F59);
  static const Color textMuted = Color(0xFF6C757D); // #6c757d
  static const Color textLight = Color(0xFF999999);

  // --- BORDERS ---
  static const Color border = Color(0xFFE0E0E0);
  static const Color borderDark = Color(0xFF30363D);

  // --- FUNCTIONAL ---
  static const Color error = Color(0xFFDC3545); // Standard Bootstrap Danger
  static const Color success = Color(0xFF28A745);
}
